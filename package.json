{
  "name": "analog-clock",
  "version": "1.0.0",
  "description": "A simple analog clock with TypeScript.",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && http-server"
  },
  "devDependencies": {
    "http-server": "latest",
    "typescript": "^5"
  }
}
